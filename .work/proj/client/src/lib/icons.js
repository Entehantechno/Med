/* Single source of truth for currency/stat emojis so the same value never
   shows two different icons across the app.
   - GEM (💉 syringe): the spendable in-app currency ("جم" / "gems"). Medical &
     unique to MED School. Used EVERYWHERE gems appear.
   - Note: 💎 (diamond) is intentionally reserved for the DIAMOND TIER and the
     Diamond Tournament — a different concept — so it must NOT be used for gems.
*/
export const GEM = "💉";      // gems currency
export const XP_STAR = "⭐";   // xp
export const FLAME = "🔥";     // streak
export const HEART = "❤️";     // hearts
export const FREEZE = "🧊";    // streak freeze
