node_modules/
dist/
*.db
*.db-shm
*.db-wal
.env
.DS_Store

# uploaded files (keep the demo image, ignore user uploads)
server/uploads/img_*

backups/
.env

# QA/test output — regenerated by the e2e suite on every run, never shipped
qa-artifacts/
qa-shots/
e2e-data/
test-results/
playwright-report/

# the chunked import payloads are the real artefact; the single-file copy is a
# 4 MB duplicate of them and is no longer written by to_import.py
tools/neurology-bank/import-payload.json
