import { db,persistNow } from "../db.js";export const getExternalAdsConfig=()=>db.prepare('SELECT * FROM external_ads_config WHERE id=1').get()||{enabled:0,provider:'none'};export function saveExternalAdsConfig(b={}){db.prepare("UPDATE external_ads_config SET enabled=?,provider=?,client_id=?,slot_id=?,placement=?,legal_note=?,updated_at=datetime('now') WHERE id=1").run(b.enabled?1:0,b.provider||'none',b.client_id||'',b.slot_id||'',b.placement||'blog',b.legal_note||'');persistNow();return getExternalAdsConfig()}export const externalAdStats=()=>db.prepare('SELECT provider,event,COUNT(*) count FROM external_ad_events GROUP BY provider,event').all();export function recordExternalAdEvent({userId=null,provider='',event='',placement='',detail={}}){db.prepare('INSERT INTO external_ad_events (user_id,provider,event,placement,detail_json) VALUES (?,?,?,?,?)').run(userId,provider,event,placement,JSON.stringify(detail||{}));persistNow();return{ok:true}}
